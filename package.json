{
  "name": "denix-ai",
  "displayName": "Denix AI",
  "description": "AI-powered code explainer",
  "version": "0.0.2",
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:ai-assistant.explainCode"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ai-assistant.explainCode",
        "title": "AI: Explain Selected Code"
      }
    ],
    "configuration": {
      "title": "Denix AI",
      "properties": {
        "denix-ai.openRouterApiKey": {
          "type": "string",
          "default": "",
          "description": "Your OpenRouter API key. Get one at https://openrouter.ai/keys",
          "markdownDescription": "Your OpenRouter API key. Get one at [openrouter.ai/keys](https://openrouter.ai/keys)"
        },
        "denix-ai.model": {
          "type": "string",
          "default": "mistralai/mistral-7b-instruct",
          "description": "The AI model to use for code explanations. Examples: mistralai/mistral-7b-instruct, meta-llama/llama-3.2-3b-instruct, google/gemini-flash-1.5",
          "markdownDescription": "The AI model to use for code explanations. Examples: `mistralai/mistral-7b-instruct`, `meta-llama/llama-3.2-3b-instruct`, `google/gemini-flash-1.5`. See [OpenRouter Models](https://openrouter.ai/models) for available models."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.105.0",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.36.0",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.102.0",
    "webpack-cli": "^6.0.1"
  }
}
